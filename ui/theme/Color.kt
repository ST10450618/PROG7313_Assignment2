package com.budgetwise.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── BudgetWise Brand Palette ───────────────────────────────────────────────
// These values are sourced directly from the Part A design specification and
// applied consistently through the Material3 colour scheme below.

val TealPrimary     = Color(0xFF1B998B)  // Deep Teal  — primary brand colour
val GreenSecondary  = Color(0xFF06D6A0)  // Vibrant Green — secondary accent
val NavyTertiary    = Color(0xFF0F4C5C)  // Navy Blue — tertiary / dark surfaces
val CoralAlert      = Color(0xFFE16162)  // Coral — overspending, errors, alerts
val YellowHighlight = Color(0xFFFDD05C)  // Soft Yellow — warnings, highlights