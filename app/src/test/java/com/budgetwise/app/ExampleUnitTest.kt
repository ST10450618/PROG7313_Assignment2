package com.budgetwise.app

import org.junit.Test
import org.junit.Assert.*

/**
 * Placeholder unit test — see HashUtilsTest, ExpenseViewModelTest,
 * GoalsViewModelTest for the actual test suite (Seth's responsibility).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
